export const reportList = ({report}) => {
  return report.all
}
export const currentReportId = ({report}) => {
  return report.currentReportId
}
export const currentReportIndex = ({report}) => {
  return report.currentReportIndex
}
